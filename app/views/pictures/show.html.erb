<div class="wrapper">
  <div>
    <%= image_tag @picture.image.url %>
  </div>
  <div>
    <%= @picture.content %>
  </div>
  <div>
    <%= @picture.user.name %>
  </div>
</div>
[<%= link_to '編集', edit_picture_path(@picture) %>]
[<%= link_to 'TOPページ', pictures_path %>]
